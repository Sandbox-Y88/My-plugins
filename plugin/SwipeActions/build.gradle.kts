version = "1.0.0"
description = "Adds swipe actions like swipe to reply or edit"

aliucord {
    changelog.set("""
        # 1.0.0
        * Initial version
    """.trimIndent())

    excludeFromUpdaterJson.set(false)
}